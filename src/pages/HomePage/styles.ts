import { Styles } from '../../types'

const styles: Styles = {
  container: {
    fontFamily: 'Montserrat',
    padding: '10px',
  },
  topRow: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
  homepageContainer: {
    height: '100%',
  },
}

export default styles
