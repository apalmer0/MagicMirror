import { CSSProperties } from 'react'

interface Styles {
  [key: string]: CSSProperties
}

const styles: Styles = {
  tipsStyle: {
    fontSize: '14px',
    marginBottom: '10px',
    marginTop: '30px',
  },
}

export default styles
