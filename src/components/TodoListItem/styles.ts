import { Styles } from '../../types'

const styles: Styles = {
  todoItem: {
    display: 'flex',
    justifyContent: 'space-between',
    marginBottom: '15px',
    marginTop: '15px',
  },
  listContent: {
    textAlign: 'right',
    width: '75%',
  },
}

export default styles
