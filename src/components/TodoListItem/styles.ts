import { Styles } from '../../types'

const styles: Styles = {
  todoItem: {
    display: 'flex',
    justifyContent: 'space-between',
    marginBottom: '15px',
    marginTop: '15px',
  },
}

export default styles
