import { Styles } from '../../types'

const styles: Styles = {
  header: {
    textAlign: 'center',
  },
  content: {
    bottom: '0',
    position: 'absolute',
    width: '98%',
  },
}

export default styles
